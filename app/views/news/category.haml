%h2= @category.name
- unless @news.blank?
  - @news.each do |news|
    %h2= news.title
    .image= image_tag news.logo.url(:thumb) if news.logo.url(:thumb)
    .content_block
      .content= strip_tags news.content
      = render partial: 'shared/tag_links', locals: { news: news }