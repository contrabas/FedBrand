%h2= t 'pages.videos.title'
- Video.all.last(3).each do |video|
  %article.video
    %figure
      %p= link_to image_tag(video.thumb), video if video.thumb
      %figcaption
        %h3= link_to video.title, video